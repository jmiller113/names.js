let name1 = "Henry";
let name2 ="Matt";
let name3 = "Ichabod Crane"; 

console.log(name1.length);

if (
     name1.length=== name2.length && name2.length === name3.length &&  name1.length === name3.length) {
    console.log(`All three names, ${name1}, ${name2}, and ${name3}, are the same length`);

} else {
    console.log("other solutions")
}